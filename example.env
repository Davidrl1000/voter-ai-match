# OpenAI Configuration
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# AWS Configuration
# Region where your DynamoDB tables and S3 bucket are located
AWS_REGION=us-east-1
# AWS credentials (or use AWS CLI profile)
AWS_ACCESS_KEY_ID=AKIA...
AWS_SECRET_ACCESS_KEY=...

# DynamoDB Tables
# Development tables (use different names for production)
CANDIDATE_POSITIONS_TABLE=candidate-positions-dev
QUESTION_BANK_TABLE=question-bank-dev
MATCH_RESULTS_TABLE=match-results-dev
AGGREGATED_STATS_TABLE=aggregated-stats-dev

# Training Configuration
# Environment: development or production
# - development: Uses gpt-4o-mini (cheaper, faster)
# - production: Uses o1-pro (better quality, expensive)
NODE_ENV=development

# Number of questions to generate (150 for dev, 1000+ for production)
QUESTION_COUNT=150

# Dry run mode: true = only process 5 candidates for testing
DRY_RUN=true

# Application Configuration
# Election day and time when results will be revealed (Costa Rica timezone: UTC-6)
# Format: YYYY-MM-DDTHH:MM:SS-06:00
ELECTION_DAY_DATETIME=2026-02-01T19:00:00-06:00